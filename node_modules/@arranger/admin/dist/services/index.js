"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.serializeToEsId = (id) => id
    .toLowerCase()
    .split('-')
    .join('_');
exports.serializeToGqlField = (field) => field.split('.').join('__');
exports.timestamp = () => new Date().toISOString();
exports.replaceBy = (arr1, arr2, operator) => {
    const cArr1 = arr1 || [];
    const cArr2 = arr2 || [];
    return [
        ...cArr2.filter(x => cArr1.find(y => operator(x, y))),
        ...cArr1.filter(x => !cArr2.find(y => operator(x, y))),
    ];
};
//# sourceMappingURL=index.js.map