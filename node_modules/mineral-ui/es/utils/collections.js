export var setFromArray = function setFromArray(array) {
  return array.reduce(function (acc, value) {
    acc.add(value);
    return acc;
  }, new Set());
};

// Recursively converts an object of arrays into an object of sets.
// Sets are faster for containment checks.
export var settify = function settify(input) {
  return Object.keys(input).reduce(function (acc, key) {
    var value = input[key];
    acc[key] = Array.isArray(value) ? setFromArray(value) : settify(value);
    return acc;
  }, {});
};

export var toArray = function toArray(value) {
  return Array.isArray(value) ? value : [value];
};